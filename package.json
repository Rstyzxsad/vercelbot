{
  "name": "chatbot-vercel",
  "version": "1.0.0",
  "description": "AI Chatbot on Vercel",
  "main": "api/chat.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.1.3"
  }
}